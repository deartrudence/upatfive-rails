<%= form_for(@portfolio_image) do |f| %>
  <% if @portfolio_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@portfolio_image.errors.count, "error") %> prohibited this portfolio_image from being saved:</h2>

      <ul>
      <% @portfolio_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Upload Image" %><br>
    <%= f.file_field :pic %>
  </div>
  <div class="actions">
    <%= f.submit %>
    <%= hidden_field_tag :portfolio_piece_id,  @portfolio_piece.id %>
  </div>
<% end %>
